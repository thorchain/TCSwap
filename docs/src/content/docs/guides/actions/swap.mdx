{/* Modifications © 2025 Horizontal Systems. */}
---
title: Performing Swaps
description: How to perform token swaps across different chains with USwap
---

import InteractiveExample from "../../../../components/InteractiveExample.astro";
import TryItYourself from "../../../../components/TryItYourself.astro";
import PlaygroundLinks from "../../../../components/PlaygroundLinks.astro";
import SwapQuoteDemo from "../../../../components/SwapQuoteDemo.astro";

USwap provides a unified interface for swapping tokens across different chains and protocols. This guide covers how to get quotes and execute swaps using both the granular and all-in-one approaches.

Before starting, make sure you understand [Core Concepts](/start/core-concepts) like [AssetValue](/start/core-concepts#assetvalue) and [Chain types](/start/core-concepts#chain-types). For detailed API methods, see the [API Reference](/guides/api-reference).

## Two Approaches for Using USwap

You can use USwap in two ways:

1. **Granular approach** - Import only what you need from individual packages (recommended for frontend):

   ```ts
   import { USwap, ThorchainPlugin, evmWallet } from "@uswap/sdk";
   ```

2. **All-in-one approach** - Import everything from the SDK (better for backend/Node.js):
   ```ts
   import { createUSwap } from "@uswap/sdk";
   ```

## Basic Swap Flow

The typical flow for performing a swap consists of these steps:

1. Initialize USwap and connect wallets
2. Get a quote for the swap
3. Approve tokens (if needed)
4. Execute the swap transaction

<TryItYourself
  title="🚀 Live Swap Demo"
  description="See USwap swaps in action with our interactive playground"
  variant="primary"
  icon="lightning"
>
  <PlaygroundLinks
    compact={true}
    title=""
    description=""
    stackblitzUrl="https://stackblitz.com/github/uswap/uswap/tree/develop/playgrounds/nextjs"
    codesandboxUrl="https://codesandbox.io/p/github/uswap/uswap/develop/playgrounds/nextjs"
  />
</TryItYourself>

## Setting Up USwap for Swaps

### Granular Approach (Frontend Recommended)

```ts twoslash
import {
  Chain,
  USwap,
  evmWallet,
  keystoreWallet,
  ThorchainPlugin,
  EVMPlugin,
} from "@uswap/sdk";

const uSwap = USwap({
  config: {
    apiKeys: {
      uSwap: "your-uswap-api-key",
      blockchair: "your-blockchair-api-key",
      walletConnectProjectId: "your-walletconnect-project-id",
    },
  },
  plugins: { ...ThorchainPlugin, ...EVMPlugin },
  wallets: { ...evmWallet, ...keystoreWallet },
});

async function connectWallets() {
  await uSwap.connectEVMWallet([Chain.Ethereum]);

  await uSwap.connectKeystore([Chain.Bitcoin], "your secret phrase here");

  const connectedWallets = Object.keys(uSwap.getAllWallets());
}
```

### All-in-one Approach (Backend)

```ts twoslash
import { Chain, createUSwap } from "@uswap/sdk";

const uSwap = createUSwap({
  config: {
    apiKeys: {
      uSwap: "your-uswap-api-key",
      blockchair: "your-blockchair-api-key",
      walletConnectProjectId: "your-walletconnect-project-id",
    },
  },
});

async function connectWallets() {
  await uSwap.connectKeystore(
    [Chain.Ethereum, Chain.Bitcoin],
    "your secret phrase here"
  );
}
```

## Getting a Swap Quote

Before executing a swap, you need to get a quote to see available routes, prices, and fees. See [getSwapQuote](/guides/api-reference#getswapquote) in the API Reference for all available parameters:

<SwapQuoteDemo />

```ts twoslash
import { USwapApi } from "@uswap/helpers/api";

async function getSwapQuote() {
  const sellAsset = "ETH.ETH";
  const buyAsset = "ETH.USDC-0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48";
  const sellAmount = "0.1";

  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset,
      buyAsset,
      sellAmount,
      sourceAddress: "0x742d35Cc6634C0532925a3b844Bc454e4438f44e",
      slippage: 1,
    });

    if (!quoteResponse.success) {
      throw new Error(`Failed to get quote: ${quoteResponse.error}`);
    }

    const routes = quoteResponse.data.routes;

    return routes;
  } catch (error) {
    throw error;
  }
}

async function getProviderQuote() {
  const { ProviderName } = await import("@uswap/sdk");
  const { USwapApi } = await import("@uswap/helpers/api");

  const quoteResponse = await USwapApi.getSwapQuote({
    sellAsset: "ETH.ETH",
    buyAsset: "ETH.USDC-0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
    sellAmount: "0.1",
    providers: [ProviderName.UNISWAP_V3],
    sourceAddress: "0x742d35Cc6634C0532925a3b844Bc454e4438f44e",
  });

  return quoteResponse.data?.routes || [];
}
```

## Executing a Swap

Once you have a quote, you can execute the swap:

### Granular Approach

```ts twoslash
import {
  AssetValue,
  Chain,
  FeeOption,
  ProviderName,
  USwapApi,
  USwap,
  evmWallet,
  EVMPlugin,
} from "@uswap/sdk";

const uSwap = USwap({
  plugins: { ...EVMPlugin },
  wallets: { ...evmWallet },
});

await uSwap.connectEVMWallet([Chain.Ethereum]);

async function executeSwap() {
  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "ETH.USDC-0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
      sellAmount: "0.1",
      sourceAddress: uSwap.getAddress(Chain.Ethereum),
      slippage: 1,
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No swap routes available");
    }

    const selectedRoute = quoteResponse.data.routes[0];

    const assetValue = await AssetValue.from({
      asset: selectedRoute.sellAsset,
      value: selectedRoute.sellAmount,
      asyncTokenLookup: true,
    });

    if (!assetValue.isGasAsset && selectedRoute.tx) {
      const isApproved = await uSwap.isAssetValueApproved(
        assetValue,
        selectedRoute.tx.from
      );

      if (!isApproved) {
        await uSwap.approveAssetValue(assetValue, selectedRoute.tx.from);
      }
    }

    const txHash = (await uSwap.swap({
      route: selectedRoute,
      feeOptionKey: FeeOption.Fast,
    })) as string;

    const txUrl = uSwap.getExplorerTxUrl({
      chain: assetValue.chain as Chain,
      txHash,
    });

    return { txHash, txUrl };
  } catch (error) {
    throw error;
  }
}
```

### All-in-one Approach

```ts twoslash
import {
  AssetValue,
  Chain,
  FeeOption,
  USwapApi,
  createUSwap,
} from "@uswap/sdk";

const uSwap = createUSwap();

await uSwap.connectKeystore([Chain.Ethereum], "your secret phrase here");

async function executeSwap() {
  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "ETH.USDC-0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
      sellAmount: "0.1",
      sourceAddress: uSwap.getAddress(Chain.Ethereum),
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No swap routes available");
    }

    const selectedRoute = quoteResponse.data.routes[0];

    const txHash = await uSwap.swap({
      route: selectedRoute,
      feeOptionKey: FeeOption.Fast,
    });

    return txHash;
  } catch (error) {
    throw error;
  }
}
```

## Cross-Chain Swaps with Chainflip

USwap supports cross-chain swaps through Chainflip:

```ts twoslash

import {
  Chain,
  FeeOption,
  ProviderName,
  USwapApi,
  USwap,
  evmWallet,
  ChainflipPlugin,
} from "@uswap/sdk";


const uSwap = USwap({
  plugins: { ...ChainflipPlugin },
  wallets: { ...evmWallet },
  config: {
    integrations: {
      chainflip: {
        brokerUrl: "https:
      },
    },
  },
});


await uSwap.connectEVMWallet([Chain.Ethereum]);


async function crossChainSwap() {
  try {

    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "BTC.BTC",
      sellAmount: "0.1",
      providers: [ProviderName.CHAINFLIP],
      sourceAddress: uSwap.getAddress(Chain.Ethereum),
      destinationAddress: "bc1qar0srrr7xfkvy5l643lydnw9re59gtzzwf5mdq",
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No cross-chain swap routes available");
    }


    const route = quoteResponse.data.routes[0];


    const txHash = await uSwap.swap({
      route,
      feeOptionKey: FeeOption.Fast,

    });


    return { txHash };
  } catch (error) {
    throw error;
  }
}
```

## THORChain Swaps

USwap also supports swaps through THORChain:

```ts twoslash
import {
  Chain,
  FeeOption,
  ProviderName,
  USwapApi,
  USwap,
  evmWallet,
  keystoreWallet,
  ThorchainPlugin,
} from "@uswap/sdk";

const uSwap = USwap({
  plugins: { ...ThorchainPlugin },
  wallets: { ...evmWallet, ...keystoreWallet },
});

async function setupAndSwap() {
  await uSwap.connectEVMWallet([Chain.Ethereum]);

  await uSwap.connectKeystore([Chain.THORChain], "your secret phrase here");

  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "THOR.RUNE",
      sellAmount: "0.1",
      providers: [ProviderName.THORCHAIN],
      sourceAddress: uSwap.getAddress(Chain.Ethereum),
      destinationAddress: uSwap.getAddress(Chain.THORChain),
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No THORChain swap routes available");
    }

    const route = quoteResponse.data.routes[0];

    const txHash = await uSwap.swap({
      route,
      feeOptionKey: FeeOption.Fast,
    });

    return { txHash };
  } catch (error) {
    throw error;
  }
}
```

## Fee Estimation

Before executing a swap, you can estimate the transaction fees:

```ts twoslash
import {
  AssetValue,
  Chain,
  FeeOption,
  USwapApi,
  USwap,
  evmWallet,
  EVMPlugin,
} from "@uswap/sdk";

const uSwap = USwap({
  plugins: { ...EVMPlugin },
  wallets: { ...evmWallet },
});

await uSwap.connectEVMWallet([Chain.Ethereum]);

async function estimateSwapFees() {
  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "ETH.USDC-0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48",
      sellAmount: "0.1",
      sourceAddress: uSwap.getAddress(Chain.Ethereum),
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No swap routes available");
    }

    const route = quoteResponse.data.routes[0];

    const assetValue = await AssetValue.from({
      asset: route.sellAsset,
      value: route.sellAmount,
      asyncTokenLookup: true,
    });

    const estimatedFee = await uSwap.estimateTransactionFee({
      type: "swap",
      feeOptionKey: FeeOption.Fast,
      params: {
        route,
        assetValue,
      },
    });

    return estimatedFee;
  } catch (error) {
    throw error;
  }
}
```

## Advanced: Swap with Custom Parameters

For more advanced use cases, you can customize swap parameters:

```ts twoslash
import {
  Chain,
  FeeOption,
  ProviderName,
  USwapApi,
  USwap,
  evmWallet,
  ThorchainPlugin,
  ChainflipPlugin,
} from "@uswap/sdk";

const uSwap = USwap({
  plugins: { ...ThorchainPlugin, ...ChainflipPlugin },
  wallets: { ...evmWallet },
});

await uSwap.connectEVMWallet([Chain.Ethereum, Chain.BinanceSmartChain]);

async function advancedSwap() {
  try {
    const quoteResponse = await USwapApi.getSwapQuote({
      sellAsset: "ETH.ETH",
      buyAsset: "BNB.BNB",
      sellAmount: "0.5",

      providers: [ProviderName.THORCHAIN, ProviderName.CHAINFLIP],

      sourceAddress: uSwap.getAddress(Chain.Ethereum),
      destinationAddress: uSwap.getAddress(Chain.BinanceSmartChain),

      slippage: 2.5,
    });

    if (!quoteResponse.success || !quoteResponse.data.routes.length) {
      throw new Error("No advanced swap routes available");
    }

    const route = quoteResponse.data.routes[0];

    const txHash = await uSwap.swap({
      route,
      feeOptionKey: FeeOption.Fast,
    });

    return { txHash };
  } catch (error) {
    throw error;
  }
}
```
