<!-- Modifications © 2025 Horizontal Systems. -->
---
interface Step {
  title: string;
  description: string;
  code?: string;
  action?: string;
  result?: string;
  tips?: string[];
}

const props = {
  description: "Follow along with this interactive tutorial",
  title: "Step-by-Step Guide",
  ...Astro.props,
};
const steps = props.steps as Step[];
---

<div class="step-guide">
  <div class="guide-header">
    <h3 class="guide-title">{props.title}</h3>
    <p class="guide-description">{props.description}</p>
    {props.playgroundUrl && (
      <a href={props.playgroundUrl} target="_blank" rel="noopener noreferrer" class="playground-link">
        <svg class="link-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <path d="M12 2L2 7l10 5 10-5-10-5z"/>
          <path d="M2 17l10 5 10-5"/>
          <path d="M2 12l10 5 10-5"/>
        </svg>
        Try in Playground
      </a>
    )}
  </div>

  <div class="guide-progress">
    <div class="progress-bar">
      <div class="progress-fill" id="progressFill" style="width: 0%"></div>
    </div>
    <div class="progress-text">
      <span id="progressText">Step 1 of {steps.length}</span>
    </div>
  </div>

  <div class="steps-container">
    {steps.map((step, index) => (
      <div class={`step ${index === 0 ? 'active' : ''}`} data-step={index + 1}>
        <div class="step-header">
          <div class="step-number">{index + 1}</div>
          <div class="step-info">
            <h4 class="step-title">{step.title}</h4>
            <p class="step-description">{step.description}</p>
          </div>
          <div class="step-status" id={`status-${index + 1}`}>
            <svg class="status-icon pending-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 6v6l4 2"/>
            </svg>
            <svg class="status-icon complete-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22,4 12,14.01 9,11.01"/>
            </svg>
          </div>
        </div>

        <div class="step-content">
          {step.code && (
            <div class="step-code">
              <div class="code-header">
                <span class="code-label">Code Example</span>
                <button class="copy-step-btn" data-code={step.code}>
                  <svg class="copy-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <rect x="9" y="9" width="13" height="13" rx="2" ry="2"/>
                    <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
                  </svg>
                  Copy
                </button>
              </div>
              <pre class="code-block"><code>{step.code}</code></pre>
            </div>
          )}

          {step.action && (
            <div class="step-action">
              <div class="action-header">
                <span class="action-label">Action Required</span>
              </div>
              <div class="action-content">
                <p>{step.action}</p>
                <button class="action-btn" data-step={index + 1}>
                  <span class="btn-icon">▶️</span>
                  Complete Step
                </button>
              </div>
            </div>
          )}

          {step.result && (
            <div class="step-result" id={`result-${index + 1}`} style="display: none;">
              <div class="result-header">
                <span class="result-label">Expected Result</span>
              </div>
              <div class="result-content">
                <p>{step.result}</p>
              </div>
            </div>
          )}

          {step.tips && step.tips.length > 0 && (
            <div class="step-tips">
              <div class="tips-header">
                <svg class="tips-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>
                </svg>
                Tips & Best Practices
              </div>
              <ul class="tips-list">
                {step.tips.map(tip => (
                  <li class="tip-item">{tip}</li>
                ))}
              </ul>
            </div>
          )}

          <div class="step-navigation">
            {index > 0 && (
              <button class="nav-btn prev-btn" data-step={index}>
                <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <polyline points="15,18 9,12 15,6"/>
                </svg>
                Previous
              </button>
            )}

            <button class="nav-btn next-btn" data-step={index + 2} style={index === steps.length - 1 ? "display: none;" : ""}>
              Next
              <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <polyline points="9,18 15,12 9,6"/>
              </svg>
            </button>
          </div>
        </div>
      </div>
    ))}
  </div>

  <div class="guide-completion" id="guideCompletion" style="display: none;">
    <div class="completion-content">
      <div class="completion-icon">🎉</div>
      <h3 class="completion-title">Congratulations!</h3>
      <p class="completion-message">You've successfully completed the tutorial. Ready to build something amazing with USwap?</p>
      {props.playgroundUrl && (
        <a href={props.playgroundUrl} target="_blank" rel="noopener noreferrer" class="completion-btn">
          <svg class="btn-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
            <path d="M2 17l10 5 10-5"/>
            <path d="M2 12l10 5 10-5"/>
          </svg>
          Build in Playground
        </a>
      )}
    </div>
  </div>
</div>

<script is:inline>
document.addEventListener('DOMContentLoaded', function() {
  let currentStep = 1;
  const stepsContainer = document.querySelector('.steps-container');
  const totalSteps = stepsContainer ? parseInt(stepsContainer.children.length.toString()) : 0;
  const progressFill = document.getElementById('progressFill');
  const progressText = document.getElementById('progressText');
  const guideCompletion = document.getElementById('guideCompletion');

  if (!progressFill || !progressText || !guideCompletion) {
    return;
  }

  function updateProgress() {
    const percentage = (currentStep / totalSteps) * 100;
    progressFill.style.width = percentage + '%';
    progressText.textContent = 'Step ' + currentStep + ' of ' + totalSteps;

    if (currentStep > totalSteps) {
      progressText.textContent = 'Complete!';
      guideCompletion.style.display = 'block';
    }
  }

  function showStep(stepNumber) {
    document.querySelectorAll('.step').forEach(function(step, index) {
      if (index + 1 === stepNumber) {
        step.classList.add('active');
      } else {
        step.classList.remove('active');
      }
    });

    currentStep = stepNumber;
    updateProgress();

    const activeStep = document.querySelector('.step.active');
    if (activeStep) {
      activeStep.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }
  }

  function completeStep(stepNumber) {
    const status = document.getElementById('status-' + stepNumber);
    const result = document.getElementById('result-' + stepNumber);

    if (status) {
      const pendingIcon = status.querySelector('.pending-icon');
      const completeIcon = status.querySelector('.complete-icon');

      if (pendingIcon) pendingIcon.style.display = 'none';
      if (completeIcon) completeIcon.style.display = 'inline';
      status.classList.add('completed');
    }

    if (result) {
      result.style.display = 'block';
    }

    setTimeout(function() {
      if (stepNumber < totalSteps) {
        showStep(stepNumber + 1);
      } else {
        currentStep = totalSteps + 1;
        updateProgress();
      }
    }, 1500);
  }

  document.querySelectorAll('.copy-step-btn').forEach(function(button) {
    button.addEventListener('click', async function() {
      const code = this.getAttribute('data-code');
      if (!code) return;

      try {
        await navigator.clipboard.writeText(code);
        const originalText = this.innerHTML;
        this.innerHTML = '<svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"><polyline points="20,6 9,17 4,12"/></svg>Copied!';
        setTimeout(function() {
          button.innerHTML = originalText;
        }, 2000);
      } catch (err) {
        console.error('Failed to copy code: ', err);
      }
    });
  });

  document.querySelectorAll('.action-btn').forEach(function(button) {
    button.addEventListener('click', function() {
      const stepAttr = this.getAttribute('data-step');
      if (!stepAttr) return;

      const stepNumber = parseInt(stepAttr);
      this.disabled = true;
      this.innerHTML = '<span class="btn-icon">⏳</span>Processing...';

      setTimeout(function() {
        completeStep(stepNumber);
        button.innerHTML = '<span class="btn-icon">✅</span>Completed';
      }, 1000);
    });
  });

  document.querySelectorAll('.nav-btn').forEach(function(button) {
    button.addEventListener('click', function() {
      const stepAttr = this.getAttribute('data-step');
      if (!stepAttr) return;

      const targetStep = parseInt(stepAttr);
      showStep(targetStep);
    });
  });

  updateProgress();
});
</script>

<style>
.step-guide {
  background: var(--sl-color-bg-nav);
  border: 1px solid var(--sl-color-hairline);
  border-radius: 0.75rem;
  overflow: hidden;
  margin: 2rem 0;
}

.guide-header {
  padding: 2rem;
  background: var(--sl-color-bg-sidebar);
  border-bottom: 1px solid var(--sl-color-hairline);
  text-align: center;
}

.guide-title {
  margin: 0 0 0.5rem 0;
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.guide-description {
  margin: 0 0 1rem 0;
  color: var(--sl-color-text-accent);
  font-size: 1rem;
}

.playground-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: var(--sl-color-accent);
  color: var(--sl-color-white);
  text-decoration: none;
  border-radius: 0.5rem;
  font-weight: 500;
  transition: all 0.2s ease;
}

.playground-link:hover {
  background: var(--sl-color-accent-high);
  transform: translateY(-1px);
}

.link-icon {
  width: 1.125rem;
  height: 1.125rem;
  stroke-width: 2;
}

.guide-progress {
  padding: 1.5rem 2rem;
  background: var(--sl-color-bg-nav);
  border-bottom: 1px solid var(--sl-color-hairline);
}

.progress-bar {
  width: 100%;
  height: 0.5rem;
  background: var(--sl-color-hairline);
  border-radius: 9999px;
  overflow: hidden;
  margin-bottom: 0.75rem;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--sl-color-accent), var(--sl-color-accent-high));
  border-radius: 9999px;
  transition: width 0.3s ease;
}

.progress-text {
  text-align: center;
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.steps-container {
  padding: 0;
}

.step {
  border-bottom: 1px solid var(--sl-color-hairline);
  transition: all 0.3s ease;
  opacity: 0.6;
}

.step.active {
  opacity: 1;
  background: var(--sl-color-bg-sidebar);
}

.step:last-child {
  border-bottom: none;
}

.step-header {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  padding: 1.5rem 2rem 1rem 2rem;
}

.step-number {
  flex-shrink: 0;
  width: 2.5rem;
  height: 2.5rem;
  background: var(--sl-color-accent);
  color: var(--sl-color-white);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 1.125rem;
}

.step.active .step-number {
  background: var(--sl-color-accent-high);
}

.step-info {
  flex: 1;
  min-width: 0;
}

.step-title {
  margin: 0 0 0.5rem 0;
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.step-description {
  margin: 0;
  color: var(--sl-color-text-accent);
  font-size: 0.9rem;
  line-height: 1.5;
}

.step-status {
  flex-shrink: 0;
}

.status-icon {
  width: 1.5rem;
  height: 1.5rem;
  stroke-width: 2;
}

.pending-icon {
  color: var(--sl-color-text-accent);
}

.complete-icon {
  color: rgb(34, 197, 94);
}

.step-content {
  padding: 0 2rem 1.5rem 5.5rem;
}

.step-code {
  margin-bottom: 1.5rem;
  border: 1px solid var(--sl-color-hairline);
  border-radius: 0.5rem;
  overflow: hidden;
}

.code-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.75rem 1rem;
  background: var(--sl-color-bg-nav);
  border-bottom: 1px solid var(--sl-color-hairline);
}

.code-label {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.copy-step-btn {
  display: flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.375rem 0.75rem;
  background: transparent;
  border: 1px solid var(--sl-color-hairline);
  border-radius: 0.25rem;
  color: var(--sl-color-text-accent);
  cursor: pointer;
  font-size: 0.75rem;
  transition: all 0.2s ease;
}

.copy-step-btn:hover {
  background: var(--sl-color-bg-accent);
  color: var(--sl-color-text);
}

.copy-icon, .check-icon {
  width: 0.875rem;
  height: 0.875rem;
  stroke-width: 2;
}

.code-block {
  padding: 1rem;
  margin: 0;
  font-size: 0.875rem;
  line-height: 1.5;
  overflow-x: auto;
  background: var(--sl-color-bg-nav);
  color: var(--sl-color-text);
}

.code-block code {
  background: none;
  padding: 0;
  font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
}

.step-action {
  margin-bottom: 1.5rem;
  background: var(--sl-color-accent-low);
  border: 1px solid var(--sl-color-accent);
  border-radius: 0.5rem;
  overflow: hidden;
}

.action-header {
  padding: 0.75rem 1rem;
  background: var(--sl-color-accent);
  color: var(--sl-color-white);
}

.action-label {
  font-size: 0.875rem;
  font-weight: 600;
}

.action-content {
  padding: 1rem;
}

.action-content p {
  margin: 0 0 1rem 0;
  color: var(--sl-color-text);
}

.action-btn {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: var(--sl-color-accent);
  color: var(--sl-color-white);
  border: none;
  border-radius: 0.375rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
}

.action-btn:hover:not(:disabled) {
  background: var(--sl-color-accent-high);
}

.action-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
}

.step-result {
  margin-bottom: 1.5rem;
  background: rgba(34, 197, 94, 0.05);
  border: 1px solid rgba(34, 197, 94, 0.2);
  border-radius: 0.5rem;
  overflow: hidden;
}

.result-header {
  padding: 0.75rem 1rem;
  background: rgba(34, 197, 94, 0.1);
  border-bottom: 1px solid rgba(34, 197, 94, 0.2);
}

.result-label {
  font-size: 0.875rem;
  font-weight: 600;
  color: rgb(34, 197, 94);
}

.result-content {
  padding: 1rem;
}

.result-content p {
  margin: 0;
  color: var(--sl-color-text);
}

.step-tips {
  margin-bottom: 1.5rem;
  background: var(--sl-color-bg-nav);
  border: 1px solid var(--sl-color-hairline);
  border-radius: 0.5rem;
  overflow: hidden;
}

.tips-header {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1rem;
  background: var(--sl-color-bg-sidebar);
  border-bottom: 1px solid var(--sl-color-hairline);
}

.tips-icon {
  width: 1rem;
  height: 1rem;
  stroke-width: 2;
  color: var(--sl-color-accent);
}

.tips-header {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.tips-list {
  padding: 1rem;
  margin: 0;
  list-style: none;
}

.tip-item {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
  font-size: 0.875rem;
  color: var(--sl-color-text);
  line-height: 1.5;
}

.tip-item:last-child {
  margin-bottom: 0;
}

.tip-item::before {
  content: '💡';
  position: absolute;
  left: 0;
  top: 0;
}

.step-navigation {
  display: flex;
  justify-content: space-between;
  margin-top: 1.5rem;
  padding-top: 1rem;
  border-top: 1px solid var(--sl-color-hairline);
}

.nav-btn {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1rem;
  background: var(--sl-color-bg-nav);
  border: 1px solid var(--sl-color-hairline);
  border-radius: 0.375rem;
  color: var(--sl-color-text);
  cursor: pointer;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.2s ease;
}

.nav-btn:hover {
  background: var(--sl-color-bg-accent);
  border-color: var(--sl-color-accent);
}

.nav-icon {
  width: 1rem;
  height: 1rem;
  stroke-width: 2;
}

.guide-completion {
  padding: 3rem 2rem;
  text-align: center;
  background: linear-gradient(135deg, var(--sl-color-accent-low), var(--sl-color-bg-nav));
}

.completion-content {
  max-width: 400px;
  margin: 0 auto;
}

.completion-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.completion-title {
  margin: 0 0 1rem 0;
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--sl-color-text);
}

.completion-message {
  margin: 0 0 2rem 0;
  color: var(--sl-color-text-accent);
  font-size: 1rem;
  line-height: 1.5;
}

.completion-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  background: var(--sl-color-accent);
  color: var(--sl-color-white);
  text-decoration: none;
  border-radius: 0.5rem;
  font-weight: 600;
  font-size: 1rem;
  transition: all 0.2s ease;
}

.completion-btn:hover {
  background: var(--sl-color-accent-high);
  transform: translateY(-2px);
}

.btn-icon {
  width: 1.125rem;
  height: 1.125rem;
  stroke-width: 2;
}

@media (max-width: 768px) {
  .guide-header {
    padding: 1.5rem;
  }

  .guide-progress {
    padding: 1rem 1.5rem;
  }

  .step-header {
    padding: 1rem 1.5rem 0.75rem 1.5rem;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }

  .step-content {
    padding: 0 1.5rem 1rem 1.5rem;
  }

  .step-navigation {
    flex-direction: column;
    gap: 0.75rem;
  }

  .nav-btn {
    justify-content: center;
  }
}
</style>
